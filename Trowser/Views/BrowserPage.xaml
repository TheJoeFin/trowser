<Page
    x:Class="Trowser.Views.BrowserPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Width="400" Height="500">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Padding="4,4" Background="{ThemeResource LayerOnMicaBaseAltFillColorDefaultBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button
                Grid.Column="0"
                Padding="8,4"
                Command="{x:Bind ViewModel.GoBackCommand}"
                ToolTipService.ToolTip="Back">
                <FontIcon FontSize="14" Glyph="&#xE76B;" />
            </Button>

            <Button
                Grid.Column="1"
                Padding="8,4"
                Command="{x:Bind ViewModel.GoForwardCommand}"
                ToolTipService.ToolTip="Forward">
                <FontIcon FontSize="14" Glyph="&#xE76C;" />
            </Button>

            <Button
                Grid.Column="2"
                Padding="8,4"
                Command="{x:Bind ViewModel.RefreshCommand}"
                ToolTipService.ToolTip="Refresh">
                <FontIcon FontSize="14" Glyph="&#xE72C;" />
            </Button>

            <TextBlock
                Grid.Column="3"
                Margin="8,0"
                VerticalAlignment="Center"
                FontWeight="SemiBold"
                Text="{x:Bind ViewModel.Name, Mode=OneWay}" />

            <Button
                Grid.Column="4"
                Padding="8,4"
                Command="{x:Bind ViewModel.PopOutCommand}"
                ToolTipService.ToolTip="Open in window">
                <FontIcon FontSize="14" Glyph="&#xE8A7;" />
            </Button>
        </Grid>

        <WebView2
            x:Name="BrowserWebView"
            Grid.Row="1"
            Source="{x:Bind ViewModel.Url, Mode=OneWay}" />
    </Grid>
</Page>
